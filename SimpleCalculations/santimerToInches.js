function convertInchesToCentimiters ([arg1]) {
    let inches = parseFloat(arg1);
    let centimeter = inches * 2.54;
    console.log=('Centimeter = ' + centimeter);
}


convertInchesToCentimiters(['2']);